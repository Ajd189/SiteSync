{
  "name": "sitesync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:support": "node scripts/support-policy.mjs",
    "predev": "npm run sync:support",
    "dev": "vite",
    "prebuild": "npm run sync:support",
    "build": "node scripts/build-netlify.mjs",
    "prebuild:sites": "npm run sync:support",
    "build:sites": "node scripts/build.mjs",
    "test": "npm run build && node --test tests/netlify-consultation.test.mjs tests/netlify-inbox.test.mjs tests/netlify-lead-digest.test.mjs tests/electric.test.mjs tests/support-policy.test.mjs",
    "check": "npm test"
  },
  "devDependencies": {
    "@netlify/functions": "^4.2.5",
    "drizzle-kit": "^0.31.10",
    "vite": "8.0.13"
  },
  "dependencies": {
    "@netlify/identity": "2.0.0",
    "drizzle-orm": "^0.45.2"
  }
}
